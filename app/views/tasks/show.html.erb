<h2>Your Task: <%= @task.title %></h2>
<p><%= @task.details %></p>
<p>Completed: <%= @task.completed %></p>

<%= link_to "Back to all tasks", tasks_path %>

<%= link_to("Edit an existing Task", edit_path) %>

<%= link_to 'Delete', delete_path, method: :delete, data: { confirm: "Are you sure?" } %>
